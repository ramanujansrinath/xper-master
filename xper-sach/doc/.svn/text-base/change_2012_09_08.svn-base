1. expose drawCustomBlank to customize the blank screen
2. only check monkey decision at the end of trial, one trial can have multiple objects with the last one being target or no target at all.
pass target index as part of the XML, -1 means no target, in which case, the monkey needs to maintain fixation.